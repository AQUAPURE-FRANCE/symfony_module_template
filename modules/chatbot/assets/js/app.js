import '../scss/app.scss';
import {Chatbot} from "./chatbot";

/** Example of fetch implementation */
/*
Chatbot.fetchData(
    null,
    new Request(
        url,
        {
            method: 'GET',
            headers: new Headers({
                    'Content-Type': 'application/json', // All Type MIME on https://developer.mozilla.org/fr/docs/Web/HTTP/Basics_of_HTTP/MIME_types/Common_types
                    'Accept-Charset' : 'utf-8'
                }
            ),
            body: JSON.stringify(''),
            credentials: 'omit',
            mode: 'cors',
            cache: 'default'
        }
    ),
    null,
    true,
    []
);
*/
alert('Your super JS has loaded! (You\'re awesome <3)');